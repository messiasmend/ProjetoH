name: ProjetoH Runner Diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test GitHub Actions
        run: |
          set -euo pipefail
          echo "GITHUB ACTIONS OK"
          uname -a
          date
